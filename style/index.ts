import { colors } from "./colors";
import { spacing } from "./spacing";
import { typography } from "./typography";
import helpersStyles from "./helpers";

export const theme = {
  colors,
  spacing,
  typography,
  helpersStyles,
} as const;